[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback: serve index.html for all routes so client-side
# routing (/, /betrieb/:slug, /gastgeber, ...) works on direct links.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
